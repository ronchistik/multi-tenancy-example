/**
 * Page configuration types
 * Mirrored from API types for use in web app
 */

export interface PageConfig {
  id: string;
  name: string;
  serializedState: string; // Craft.js serialized JSON
}

